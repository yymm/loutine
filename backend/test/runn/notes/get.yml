desc: Get notes by date range
runners:
  req: http://localhost:8787
needs:
  post: ./test/runn/notes/post.yml
steps:
- req:
    /api/v1/notes?start_date=2025-01-01&end_date=2025-12-31:
      get:
        body: null
  test: |
    current.res.status == 200
    && current.res.headers["Content-Type"][0] == "application/json"
