desc: Get purchase by ID
runners:
  req: http://localhost:8787
needs:
  post: ./test/runn/purchases/post.yml
steps:
- req:
    /api/v1/purchases/1:
      get:
        body: null
  test: |
    current.res.status == 200
    && current.res.headers["Content-Type"][0] == "application/json"
