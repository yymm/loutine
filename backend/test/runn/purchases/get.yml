desc: Get purchases by date range
runners:
vars:
  authKey: '{{ env "CUSTOM_AUTH_KEY" "" }}'
  req: http://localhost:8787
steps:
- req:
    /api/v1/purchases?start_date=2025-01-01&end_date=2025-12-31:
      get:
        headers:
          X-Custom-Auth-Key: '{{ vars.authKey }}'
        body: null
  test: |
    current.res.status == 200
    && current.res.headers["Content-Type"][0] == "application/json"
