desc: Get latest links by cursor
runners:
  req: http://localhost:8787
needs:
  post: ./post.yml
steps:
- req:
    /api/v1/links/latest:
      get:
        body: null
  test: |
    current.res.status == 200
    && current.res.headers["Content-Type"][0] == "application/json"
